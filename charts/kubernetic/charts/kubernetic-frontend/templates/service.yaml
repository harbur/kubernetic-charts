apiVersion: v1
kind: Service
metadata:
  name: kubernetic-frontend
  labels:
{{ include "kubernetic-frontend.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "kubernetic-frontend.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
