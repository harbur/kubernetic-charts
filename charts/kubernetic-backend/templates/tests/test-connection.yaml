apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubernetic-backend.fullname" . }}-test-connection"
  labels:
{{ include "kubernetic-backend.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kubernetic-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
